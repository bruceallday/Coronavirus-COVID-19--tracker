import { makeStyles } from '@material-ui/styles/'

export const useStyles = makeStyles({
    legend: {
        width: 500,
        height: 200,
        marginTop: '30%',
        marginLeft: '1%',
    },

    colourSq: {
        width: 15,
        height: 15,
        opacity: 0.7,
        marginLeft: 20
    }
})