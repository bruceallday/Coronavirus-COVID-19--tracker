import { makeStyles } from '@material-ui/styles/'

export const useStyles = makeStyles({
    root: {
        width: '29vh',
        height: '15vh',
        margin: 10,
        padding: 5,
        color: 'white',
        backgroundColor: '#363636',
    },
});